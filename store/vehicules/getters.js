/**
 * Get vehicules list
 * @param state
 * @returns {array|*}
 */
export const getVehicules = (state) => state.vehicules;

/**
 * Get single vehicule
 * @param state
 * @returns {object|*}
 */
export const getVehicule = (state) => state.vehicule;